@model Predictive_Lifestyle_Project.Models.UserInfo

@{
    ViewData["Title"] = "User Info";
}

<div style="display: flex; justify-content: center; align-items: center; height: 100vh;">
    <form asp-action="Index" method="post" style="text-align: center; width: 300px;">
        <h2>Enter Your Info</h2>
        <div class="form-group mb-3">
            <label asp-for="Age"></label>
            <input asp-for="Age" class="form-control" placeholder="Enter age" />
        </div>

        <div class="form-group mb-3">
            <label asp-for="Weight"></label>
            <input asp-for="Weight" class="form-control" placeholder="Enter weight" />
        </div>

        <div class="form-group mb-3">
            <label asp-for="Height"></label>
            <input asp-for="Height" class="form-control" placeholder="Enter height" />
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>

        @if (ViewBag.Message != null)
        {
            <p class="mt-3">@ViewBag.Message</p>
        }
    </form>
</div>
